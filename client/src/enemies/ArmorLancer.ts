import { armedDirectionalDef } from "./directionalEnemy";

// Holds a lance in hand and thrusts it. Mirrors the server's ArmorLancer.
export const armorLancer = armedDirectionalDef("armor-lancer", "Armor Lancer", "soldier-lance");
