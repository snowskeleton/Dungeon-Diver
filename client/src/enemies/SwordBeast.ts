import { armedDirectionalDef } from "./directionalEnemy";

// Holds a blade in hand and swings it. Mirrors the server's SwordBeast.
export const swordBeast = armedDirectionalDef("sword-beast", "Sword Beast", "beast-sword");
