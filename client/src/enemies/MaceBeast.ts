import { armedDirectionalDef } from "./directionalEnemy";

// Holds a mace in hand and swings it. Mirrors the server's MaceBeast.
export const maceBeast = armedDirectionalDef("mace-beast", "Mace Beast", "beast-mace");
