import { makeSheetEnemyDef } from "./sheetEnemy";

// A small horizontal-facing critter. Mirrors the server's Spider.
export const spider = makeSheetEnemyDef("spider", { name: "Spider", frameRate: 10 });
