import { smallDirectionalDef } from "./directionalEnemy";

// The lunging snake. Mirrors the server's Fang (the lunge reuses the move frames).
export const fang = smallDirectionalDef("fang", "Fang");
