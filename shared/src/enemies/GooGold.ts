import { EnemyConfig } from "./base";

export const GOO_GOLD_CONFIG: EnemyConfig = {
  maxHp: 100,
  speed: 60,
  aggroRadius: 180,
  attackRadius: 14,
  attackDamage: 18,
  attackCooldownMs: 1000,
  knockbackResistance: 8,
};
