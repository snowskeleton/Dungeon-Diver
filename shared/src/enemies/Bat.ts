import { EnemyConfig } from "./base";

export const BAT_CONFIG: EnemyConfig = {
  maxHp: 30,
  speed: 110,
  aggroRadius: 200,
  attackRadius: 12,
  attackDamage: 8,
  attackCooldownMs: 900,
  knockbackResistance: 0,
};
