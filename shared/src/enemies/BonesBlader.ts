import { EnemyConfig, PLACEHOLDER_ENEMY_CONFIG } from "./base";

// TODO: untuned — see PLACEHOLDER_ENEMY_CONFIG in base.ts.
export const BONES_BLADER_CONFIG: EnemyConfig = { ...PLACEHOLDER_ENEMY_CONFIG, facingMode: "directional" };
